@model IEnumerable<DataLayer.Page>
@{
    ViewBag.Title = "جستجو";
}

<section class="last-news-section border-radius margin-top-30">
    @if (Model.Count() == 0)
    {
        <header>
            چیزی یافت نشد 
        </header>
    }

    @if (ViewBag.search == "")
    {
        <header>
            لطفا عبارت را جستجو کنید 
        </header>
    }
    else
    {
        <header><i class="icons icon-doc-2"></i>  <h3> "@Model.Count()" مقاله با عبارت "@ViewBag.search" یافت شد</h3> </header>

        <ul>
            @foreach (var item in Model)
            {
                <li>
                    <img class="border-radius" src="~/PageImages/@item.ImageName">
                    <h2> <a href="/News/@item.PageID/@item.Title"> @item.Title </a></h2>
                    <p>
                        @item.ShortDescription
                    </p>
                    <span class="date-publish"><i class="fa fa-calendar"></i> انتشار : @item.CreateDate.ToShamsi() </span>
                    <a href="/News/@item.PageID/@item.Title" class="more-link"><i class="icons icon-more"></i></a>
                </li>
            }
        </ul>
    }
   
</section>

